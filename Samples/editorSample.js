import {
    editor, console, file, scene, dialogWindowManager, math,
    Float2, Float3, Float4, Bool3, ColorRGBA, Trans, Quaternion,
} from "editorApi"

/** @type {MenuPath} */
export const menuPath = ""

/** @type {EditorExecute} */
export const execute = () => { }
