import {
    // modules
    editor,
    console,
    file,
    scene,
    dialogWindowManager,
    math,

    // basic data types
    Float2,
    Float3,
    Float4,
    Bool3,
    ColorRGBA,
    Trans,
    Quaternion,
} from "editorApi"
/** commonly used types if needed
@import {
    // SceneObject
    SceneFolder,
    SceneItem,
    SceneSettings,

    // ItemComponent
    AudioPlayer,
    Camera,
    Collection,
    Decal,
    Executor,
    ItemLink,
    Joint,
    Light,
    ParticleEmitter,
    PhysicsObject,
    Renderer,
    RoadGenerator,
    Settings,
    TerrainStamp,
    Trigger,
    WayPath,
    WayPoint,

    // SettingsComponent
    AssetReference,
    BackgroundMusic,
    Cloud,
    CollectionPrototype,
    CustomBall,
    Environment,
    Fog,
    Lens,
    Lighting,
    Landscaping,
    MapInfo,
    Sky,
    Terrain,
} from "editor:alias" 
*/

/** @type {MenuPath} */
export const menuPath = ""

/** @type {EditorExecute} */
export const execute = () => {}
