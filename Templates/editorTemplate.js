import {
    // modules
    editor,
    console,
    file,
    scene,
    dialogWindowManager,
    math,

    // basic data types
    Float2,
    Float3,
    Float4,
    Bool3,
    ColorRGBA,
    Trans,
    Quaternion,
} from "editorApi"

/** @type {MenuPath} */
export const menuPath = ""

/** @type {EditorExecute} */
export const execute = () => {}
